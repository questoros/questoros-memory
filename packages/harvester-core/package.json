{
  "name": "@questoros-memory/harvester-core",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic and model-backed organizational-intelligence harvest orchestration",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint . --max-warnings=0",
    "test": "vitest run"
  },
  "dependencies": {
    "@questoros-memory/memory-core": "workspace:*",
    "@questoros-memory/reasoning-provider": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0",
    "vitest": "^4.1.10"
  }
}
