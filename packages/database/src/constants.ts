/**
 * Phase 2 embedding contract constants.
 * These define the fixed embedding model properties.
 */
export const EMBEDDING_MODEL_ID = 'amazon.titan-embed-text-v2:0';
export const EMBEDDING_DIMENSIONS = 1024;
export const EMBEDDING_NORMALIZE = true;
