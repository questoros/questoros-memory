name: Phase 8E format artifact

on:
  push:
    branches:
      - feat/phase-8-remote-mcp-demo
    paths:
      - 'README.md'
      - 'docs/final-submission-review.md'
      - 'docs/private-judge-testing-template.md'
      - '.github/workflows/phase8e-format-artifact.yml'

permissions:
  contents: write

jobs:
  format-artifact:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: feat/phase-8-remote-mcp-demo
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - uses: pnpm/action-setup@v6
        with:
          version: 11.16.0
          run_install: false
      - run: pnpm install --frozen-lockfile
      - name: Format Phase 8E files
        run: |
          pnpm exec prettier --write README.md docs/final-submission-review.md docs/private-judge-testing-template.md
      - name: Commit exact formatter output
        shell: bash
        run: |
          if git diff --quiet; then
            echo 'No formatting changes required.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add README.md docs/final-submission-review.md docs/private-judge-testing-template.md
          git commit -m 'fix(phase8): format final submission package'
          git push origin HEAD:feat/phase-8-remote-mcp-demo
