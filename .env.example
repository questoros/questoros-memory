# CockroachDB target database. Never commit the real value.
DATABASE_URL="postgresql://USERNAME:PASSWORD@HOST:26257/questoros_memory?sslmode=verify-full"

DATABASE_NAME="questoros_memory"

# Exact comma-separated browser origins allowed to call the Memory API.
# Do not use wildcards. Example: https://memory.questoros.ai
MEMORYOS_PORTAL_ORIGINS=""

# Public, credential-free API endpoint embedded in the standalone portal build.
# This is used for health checks and the separate standalone MemoryOS identity flow.
MEMORYOS_PUBLIC_API_BASE_URL=""

# Standalone MemoryOS identity and branded email links.
# This must be the MemoryOS portal origin, not the QuestorOS application origin.
MEMORYOS_PORTAL_BASE_URL="https://memory.questoros.ai"

# Provider-neutral HTTPS webhook used to send verification, reset, and invitation emails.
# The webhook receives only the recipient, template name, branded action URL, and
# approved organization/inviter labels. It must never return or log database credentials.
MEMORYOS_AUTH_EMAIL_WEBHOOK_URL=""
MEMORYOS_AUTH_EMAIL_WEBHOOK_TOKEN=""

# Embedding provider contract (Phase 4). Defaults never invoke AWS from CI/unit tests.
EMBEDDING_PROVIDER="amazon-bedrock"
EMBEDDING_MODEL_ID="amazon.titan-embed-text-v2:0"
EMBEDDING_DIMENSIONS="1024"
EMBEDDING_NORMALIZE="true"
AWS_BEDROCK_REGION="us-west-2"

EMBEDDING_AUTO_ON_WRITE="false"
EMBEDDING_MAX_INPUT_CHARACTERS="20000"
EMBEDDING_TIMEOUT_MS="10000"
EMBEDDING_MAX_ATTEMPTS="3"

# Structured reasoning defaults to mock and never invokes AWS unless explicitly enabled.
REASONING_PROVIDER="mock"
REASONING_MODEL_ID="mock-structured-v1"
REASONING_REGION="us-west-2"
REASONING_ALLOW_LIVE_CALLS="false"
REASONING_MAX_INPUT_CHARACTERS="24000"
REASONING_MAX_OUTPUT_TOKENS="2048"
REASONING_TIMEOUT_MS="15000"

# Do not set AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_SESSION_TOKEN,
# AWS_BEARER_TOKEN_BEDROCK, or AWS_PROFILE in tracked files.
# Local developers may use ignored AWS CLI/SSO configuration.
# Deployed code must use an IAM role.

# Integration tests are opt-in.
RUN_DATABASE_INTEGRATION_TESTS="false"
