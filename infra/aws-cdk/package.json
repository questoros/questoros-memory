{
  "name": "@questoros-memory/aws-cdk",
  "version": "0.1.0",
  "private": true,
  "description": "AWS CDK staging infrastructure for the deployed QuestorOS Memory API; manual deploy and teardown require explicit approval",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "synth": "node scripts/synth.mjs",
    "synth:verify": "node scripts/verify-assembly.mjs",
    "smoke:staging": "node scripts/staging-smoke.mjs",
    "smoke:phase7-harvest": "node scripts/phase7-governed-harvest-smoke.mjs",
    "deploy": "node -e \"console.error('Deployment is blocked until explicit approval. Use the reviewed command in docs/phase-6-aws-cost-and-teardown.md.'); process.exit(1)\"",
    "teardown": "node -e \"console.error('Teardown is blocked until explicit approval. Use the reviewed stack-only command in docs/phase-6-aws-cost-and-teardown.md.'); process.exit(1)\""
  },
  "dependencies": {
    "aws-cdk-lib": "^2.200.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  }
}
